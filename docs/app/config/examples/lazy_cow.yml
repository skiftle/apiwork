title: Custom Hash Responses
description: Using respond_with with plain hashes instead of ActiveRecord models
order: 8

scenarios:
  - title: Health check
    description: Returns system health status
    order: 1
    method: GET
    path: /lazy_cow/status/health

  - title: System statistics
    description: Returns usage metrics
    order: 2
    method: GET
    path: /lazy_cow/status/stats
